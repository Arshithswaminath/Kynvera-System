pytest==7.4.0
pytest-cov==4.1.0
flake8==6.0.0
pypdf>=5.0.0

# Optional: HR daily UI snapshot PDF (pip install playwright && playwright install chromium)
playwright>=1.40.0

# Load-testing only — not required for `pytest tests/`, used by loadtest/locustfile.py
locust>=2.20.0
